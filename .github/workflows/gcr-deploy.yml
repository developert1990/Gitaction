name: Deploy

on:
  pull_request:
    branches:
      - staging
jobs:
  deploy:
    name: This is deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install